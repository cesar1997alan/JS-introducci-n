// Objeto Math

let resultado;

resultado = Math.PI;
resultado = Math.round(4.7);
resultado = Math.ceil(4.7); // siempre redondea hacia arriba 
resultado = Math.floor(4.7); // siempre redondea hacia abajo 
resultado = Math.sqrt(150); // raiz cuadrada 
resultado = Math.abs(-4.7); // convierte número negativos en positivos 
resultado = Math.min(4, 7, 8, 4, 9, 3, 1, 10); // toma el número más pequeño de un listado 
resultado = Math.max(4, 7, 8, 4, 9, 3, 1, 10); //nos da el número más alto 
resultado = Math.random(); // nos da números aleatorios, pero no pasa de 1
resultado = Math.floor(Math.random() * 30); // condicionamos para que nos arroje enteros



console.log(resultado);